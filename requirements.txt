PyQt6>=6.0.0
pypdf>=3.0.0
cryptography>=41.0.0

# Dipendenze di sviluppo/test: installare con `pip install -e .[dev]`
# (vedi setup.py). pytest-qt in particolare va in conflitto con il mock
# di PyQt6 usato in tests/conftest.py se importato prima di lui.
